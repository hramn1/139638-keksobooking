'use strict';

window.backend = (function () {
  var load = function () {
    var xhr = new XMLHttpRequest();
  };

  var save = function () {
  var xhr = new XMLHttpRequest();
  };

  return {
    save: save,
    load: load,
  }
})();
